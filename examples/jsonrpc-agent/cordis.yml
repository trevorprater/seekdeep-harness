# Unattended coding-agent deployment for the bundled seekdeep-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@seekdeep-ai/seekdeep-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.SEEKDEEP_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.SEEKDEEP_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@seekdeep-ai/seekdeep-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@seekdeep-ai/seekdeep-subprocess-local'

- id: bash
  name: '@seekdeep-ai/seekdeep-bash-local'
  config:
    cwd: !!js process.env.SEEKDEEP_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@seekdeep-ai/seekdeep-agent-spine-demo'
  config:
    persona: !!js process.env.SEEKDEEP_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@seekdeep-ai/seekdeep-session-persistence-jsonl'
  config:
    root: !!js process.env.SEEKDEEP_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.SEEKDEEP_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@seekdeep-ai/seekdeep-session-checkpoint-policy'

- id: subagent
  name: '@seekdeep-ai/seekdeep-subagent'

- id: subagent-spawn-in-process
  name: '@seekdeep-ai/seekdeep-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@seekdeep-ai/seekdeep-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@seekdeep-ai/seekdeep-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@seekdeep-ai/seekdeep-fs-local'
  config:
    cwd: !!js process.env.SEEKDEEP_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@seekdeep-ai/seekdeep-fs-observation-policy'

- id: tool-fs
  name: '@seekdeep-ai/seekdeep-tool-fs'

- id: token-meter
  name: '@seekdeep-ai/seekdeep-token-meter'

- id: compaction-basic
  name: '@seekdeep-ai/seekdeep-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
