# The CHILD runtime for the SDK subagent composition test: a complete
# stdio JSON-RPC harness whose scripted model echoes its process cwd. The
# parent's subagent-sdk backend spawns this composition per run; stdout is
# reserved for JSON-RPC frames.
- id: sdk-jsonrpc-server
  name: '@seekdeep-ai/seekdeep-sdk-jsonrpc-server'

- id: child-mock-llm
  name: './child-mock-llm.ts'

- id: agent-core
  name: '@seekdeep-ai/seekdeep-agent-spine-demo'
  config:
    persona: 'Echo where you run.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

# The child persists its own session log beside the parent's (distinct root),
# so the driving e2e can inspect both transcripts after the run.
- id: sessions
  name: '@seekdeep-ai/seekdeep-session-persistence-jsonl'
  config:
    root: !!js process.env.SEEKDEEP_SESSION_ROOT ?? './.child-sessions'
    compression: none

- id: session-checkpoints
  name: '@seekdeep-ai/seekdeep-session-checkpoint-policy'

# bash-local executes through the subprocess seam.
- id: subprocess
  name: '@seekdeep-ai/seekdeep-subprocess-local'

- id: bash
  name: '@seekdeep-ai/seekdeep-bash-local'
  config:
    cwd: !!js process.env.SEEKDEEP_CWD ?? process.cwd()
