# Minimal live counterpart for the glob-sampling snapshot composition.
- id: llm-deepseek
  name: '@seekdeep-ai/seekdeep-llm-deepseek'
  config:
    models:
      - id: deepseek-v4-pro

- id: subprocess
  name: '@seekdeep-ai/seekdeep-subprocess-local'

- id: bash
  name: '@seekdeep-ai/seekdeep-bash-local'

- id: acp-agent
  name: '@seekdeep-ai/seekdeep-acp-demo'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro
    persistenceRoot: !!js process.env.SEEKDEEP_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
    persistenceCompression: !!js "process.env.SEEKDEEP_SNAPSHOT === undefined ? 'zstd' : 'none'"
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false
    persona: You are a concise snapshot agent working in {{cwd}}.

- id: tool-fs-search
  name: '@seekdeep-ai/seekdeep-tool-fs-search'
  config:
    sampleOverCapGlobResults: true
    globMaxResults: 4
