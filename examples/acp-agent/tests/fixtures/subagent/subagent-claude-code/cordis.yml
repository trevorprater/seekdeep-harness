# Test-only composition of both public opt-in providers and foreground tools.
# The owning e2e boots this tree but never invokes a model or product process.
- id: fixture
  name: './fixture.ts'

- id: subagent
  name: '@seekdeep-ai/seekdeep-subagent'

- id: subprocess
  name: '@seekdeep-ai/seekdeep-subprocess-local'

- id: subagent-codex
  name: '@seekdeep-ai/seekdeep-subagent-codex'

- id: subagent-claude-code
  name: '@seekdeep-ai/seekdeep-subagent-claude-code'

- id: tool-subagent-codex
  name: '@seekdeep-ai/seekdeep-tool-subagent'
  config:
    provider: codex
    toolName: subagent_codex
    enableRunInBackground: false
    maxDepth: 'provider-managed'

- id: tool-subagent-claude-code
  name: '@seekdeep-ai/seekdeep-tool-subagent'
  config:
    provider: claude-code
    toolName: subagent_claude_code
    enableRunInBackground: false
    maxDepth: 'provider-managed'

- id: agent-spine
  name: '@seekdeep-ai/seekdeep-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: mock
        model: mock-delegate
        cwd: !!js process.cwd()
    persona: 'This composition test must not start a model turn.'
    workspaceContext: false

- id: persistence
  name: '@seekdeep-ai/seekdeep-session-persistence-jsonl'
  config:
    root: './.sessions'

- id: checkpoint-policy
  name: '@seekdeep-ai/seekdeep-session-checkpoint-policy'
