{
  "steps": [
    {
      "op": "initialize"
    },
    {
      "op": "newSession"
    },
    {
      "op": "prompt",
      "text": "The sandbox already denied writing /tmp/seekdeep-escalated.txt earlier (it is outside this workspace). Retry it now exactly once: one single bash call with the command printf 'escalated\\n' > /tmp/seekdeep-escalated.txt && cat /tmp/seekdeep-escalated.txt && rm /tmp/seekdeep-escalated.txt, with sandbox_permissions set to danger-full-access and the justification 'the user asked to write a file outside the workspace'. Do not run it without sandbox_permissions first. I will reject the permission prompt; after the rejection, do not retry and do not work around it \u2014 explain in one short sentence and stop."
    }
  ],
  "permissionAnswers": [
    {
      "kind": "reject_once"
    }
  ]
}
