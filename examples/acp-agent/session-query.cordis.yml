# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@seekdeep-ai/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@seekdeep-ai/seekdeep-tool-session-query'
          - id: timeout-policy
            name: '@seekdeep-ai/seekdeep-tool-call-timeout-policy'
