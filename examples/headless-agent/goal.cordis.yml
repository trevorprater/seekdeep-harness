# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@seekdeep-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@seekdeep-ai/seekdeep-goal'
          - id: tool-goal
            name: '@seekdeep-ai/seekdeep-tool-goal'
