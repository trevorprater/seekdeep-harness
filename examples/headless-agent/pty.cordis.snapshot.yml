# Keyless opt-in PTY composition for the headless stream-json snapshot.
- id: base
  name: '@seekdeep-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@seekdeep-ai/seekdeep-llm-deepseek'
        disabled: true
      - insert:
          - id: sandbox-policy
            name: '@seekdeep-ai/seekdeep-sandbox-policy'
            config:
              mode: danger-full-access
          - id: pty
            name: '@seekdeep-ai/seekdeep-terminal'
          - id: pty-snapshot-backend
            name: '../acp-agent/pty-snapshot-backend.mjs'
          - id: tool-terminal
            name: '@seekdeep-ai/seekdeep-tool-terminal'
            config:
              maxResultBytes: 64
          - id: llm-replay
            name: '@seekdeep-ai/seekdeep-llm-replay'
