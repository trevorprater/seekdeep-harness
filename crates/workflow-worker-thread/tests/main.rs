//! One test binary per crate: every top-level test file is a module here.

mod built_worker_e2e;
mod host_lifecycle_parity;
mod integration_parity;
mod meta_parity;
mod protocol_json_parity;
mod runtime_session_parity;
mod start_validation_parity;
mod worker_session_parity;
