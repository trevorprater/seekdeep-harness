//! One test binary per crate: every top-level test file is a module here.

mod fixtures_parity;
mod remote_parity;
mod settings_scope_parity;
mod snapshot_parity;
mod translate_parity;
mod workspaces_parity;
