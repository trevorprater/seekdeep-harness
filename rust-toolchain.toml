[toolchain]
channel = "1.93.1"
components = ["clippy", "rustfmt"]
# Client bundles, the compiled Node runtime, and the release pipeline build for the browser
# and Node WebAssembly targets; rustup installs the target alongside the pinned toolchain.
targets = ["wasm32-unknown-unknown"]
profile = "minimal"

